apiVersion: v1
kind: Service
metadata:
  name: {{ include "cast-db.fullname" . }}
  labels:
    {{- include "cast-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cast-db.selectorLabels" . | nindent 4 }}
