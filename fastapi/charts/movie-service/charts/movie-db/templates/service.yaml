apiVersion: v1
kind: Service
metadata:
  name: {{ include "movie-db.fullname" . }}
  labels:
    {{- include "movie-db.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "movie-db.selectorLabels" . | nindent 4 }}
